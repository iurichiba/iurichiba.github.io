import React from 'react';
import ReactDOM from 'react-dom';
import Home from './home';

const root = <React.StrictMode><Home /></React.StrictMode>
ReactDOM.render(root, document.getElementById('root'));
